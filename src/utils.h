#ifndef ICEGAMMON_UTILS_H_
#define ICEGAMMON_UTILS_H_
	#include <numeric> 
	#include <algorithm>
	#include <cmath>
	#include <vector>
	double utils_mean(std::vector<double> v);
	double utils_stddev(std::vector<double> v);
#endif